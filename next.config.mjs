/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Uncomment this to export static web output for static hosting
    reactStrictMode: true,
};

export default nextConfig;
